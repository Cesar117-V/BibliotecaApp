name: biblioteca_app
description: "Aplicación de biblioteca para administrar libros y autores."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.2
  path: ^1.9.0
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.3.5
  sqflite_common_ffi_web: ^0.3.0
  fl_chart: ^0.66.2
  intl: ^0.18.1  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
